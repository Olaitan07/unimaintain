services:
  backend:
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
  frontend:
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 256M
